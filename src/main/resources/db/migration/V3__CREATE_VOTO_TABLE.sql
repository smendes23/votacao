CREATE TABLE VOTO (
                      ID_PAUTA BIGINT NOT NULL,
                      CPF VARCHAR(255) NOT NULL,
                      VOTO VARCHAR(255) NOT NULL,
                      PRIMARY KEY (CPF)
);